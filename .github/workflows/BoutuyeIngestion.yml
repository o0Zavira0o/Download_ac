      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests

      - name: Run Artifact Pipeline
        run: python artifact_pipeline.py --url "لینک_یوتیوب_شما"
